services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    # 👇 빌드 플랫폼을 지정합니다.
    platform: linux/amd64
    container_name: rps-game
    ports:
      - '3005:3005'
    restart: unless-stopped
