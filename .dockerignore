# Docker 빌드 시 포함하지 않을 파일 및 폴더 목록

# 의존성 폴더 (컨테이너 안에서 새로 설치하므로 제외)
node_modules

# 빌드 결과물 (컨테이너 안에서 새로 생성하므로 제외)
.next
dist

# 로그 파일
npm-debug.log
*.log

# Docker 관련 파일
Dockerfile
docker-compose.yml

# 기타
.env
